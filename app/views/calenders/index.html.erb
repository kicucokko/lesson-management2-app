<%= render "shared/header"%>

<div class="container">
  <div class="row">
    <div class="col-12 text-center">
      <%= link_to "予約画面",user_path(current_user.id), class: 'room-btn' %>
      <p>3ヶ月先まで予約することができます。</p>
    </div>
    <div class="col-12 mt-3">
      <%= month_calendar events: @calenders do |date, calender| %>
       <%= date.day %>
      <% end %>
    </div>
  </div>
</div>